int maximumSubarraySum(int n, vector<int> &v) {
    // Write your code here.
   sort(v.begin(),v.end());
   int ans=0;
   for(auto it:v){
       if(it>0){
           ans+=it;
       }
   }
   return ans;
}
